[data]
email = {{ or (env "GIT_AUTHOR_EMAIL") (env "EMAIL") "devcontainer@example.local" | quote }}
name = {{ or (env "GIT_AUTHOR_NAME") (env "USER") "Dev Container" | quote }}

[git]
autoCommit = false
autoPush = false

[edit]
command = "nvim"
