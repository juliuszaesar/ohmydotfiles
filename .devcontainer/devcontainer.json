{
  "name": "ohmydotfiles",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "remoteEnv": {
    "GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}"
  },
  "postCreateCommand": "./setup",
  "customizations": {
    "vscode": {
      "extensions": []
    }
  }
}
